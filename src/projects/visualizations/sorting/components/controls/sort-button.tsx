import * as React from 'react'

import {Button} from '@chakra-ui/react'

const SortButton = () => {
  return (
    <Button aria-label="shuffle-array" minW={32}>
      Sort!
    </Button>
  )
}

export default SortButton
